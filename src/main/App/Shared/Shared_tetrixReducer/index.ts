/**
 * Tetrix Reducer - Re-export from main reducers directory
 * This allows Shared components to import the reducer without upward imports
 */

export { tetrixReducer, initialState } from '../../reducers';

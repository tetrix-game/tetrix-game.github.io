/**
 * App Version Configuration
 *
 * This version is checked against saved game data.
 * Any mismatch results in the save being treated as corrupted,
 * forcing a fresh start with default state.
 *
 * IMPORTANT: Increment this whenever making changes that would
 * make old saves incompatible (even minor changes).
 */
export const APP_VERSION = '1.2.1';
